# Fill these in when you have them. The app starts without them;
# the staff list stays empty until they are set.
#
# URL should be the project origin only, not /rest/v1
#   https://xxxxxxxx.supabase.co
#
# Tables used: coworkers (staff) and breaks.
#
# Prefer the service role key on the server PC if RLS blocks the anon key.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# MANAGER_PIN=9119
